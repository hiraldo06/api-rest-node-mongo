# Rest Api con NodeJS y MongoDB

## Para ejecutar el proyecto

## Ejecutamos el comando de docker:
```bash
    
        docker build -t nodejs_webserver # crea la imagen
        docker-compose up #sube el proyecto y crea la base de datos
```
